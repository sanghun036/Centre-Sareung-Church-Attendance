import streamlit as st

# 페이지 기본 설정
st.set_page_config(page_title="사릉중앙 출석부", layout="centered")

# 상단 제목 디자인
st.title("⛪ 사릉중앙교회 출석 관리 시스템")
st.info("선생님들을 위한 간편 출석체크 앱입니다.")

# 1. 목양반 선택 드롭다운
group = st.selectbox("📌 목양반을 선택하세요", ["1목장", "2목장", "3목장", "4목장"])

# 2. 날짜 선택
date = st.date_input("📅 날짜 선택")

st.divider()

# 3. 명단 UI 구성 (현재는 예시이며, 나중에 구글 시트와 연결됩니다)
st.subheader(f"📋 {group} 명단")
students = ["홍길동", "김세한", "정혜림", "이영희"]

for s in students:
    # 한 줄에 이름과 버튼 2개를 배치
    cols = st.columns([2, 1, 1])
    
    with cols[0]:
        st.write(f"**{s}**")
        
    with cols[1]:
        if st.button("출석", key=f"att_{s}"):
            st.success(f"{s} 완료")
            # 추후 여기에 구글 시트 저장 코드가 들어갑니다.
            
    with cols[2]:
        if st.button("지각", key=f"late_{s}"):
            st.warning(f"{s} 지각")
            # 추후 여기에 구글 시트 저장 코드가 들어갑니다.

# 사이드바 메뉴 (관리용)
st.sidebar.title("⚙️ 관리 메뉴")
if st.sidebar.button("데이터 동기화"):
    st.sidebar.write("구글 시트와 연결을 시도합니다...")
